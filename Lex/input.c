// Sample c pgm to generate tokens
#include <stdio.h>
int i=0; // identifiers
char c[100];
double a=2.000004;
float k=2.345;

int n = 9; 
    printf("%d", fib(n)); // printing on to terminal
    getchar();

 int fib(int n) 
{ 
    if (n <= 1) 
        return n; 
    return fib(n-1) + fib(n-2); 
} 
while(i=0)
{
    if(i)
    	continue;  // continue while loop from start
    if(k>=10)
    	break;    // come out of while loop
    else
    {
    	k++;
    	a--;
    }
}
 float j = k/i;
 switch(n)       // switch statements
 {
 	case 1: printf("case-1");
 	        break;
 	case 2: printf("case-2");
 	        break;
 	default:printf("Default\n");
}
@serft;